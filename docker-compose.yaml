version: '2'
services:
  web:
    build: ./web-server
    command: node ./dist/src/index.js
    ports:
      - "127.0.0.1:9999:9999"

    volumes:
      - ./vismooc-web-server:/src
    depends_on:
      - db
      - redis
    networks:
      - front-tier
      - back-tier

  data:
    build: ./data-server
    command: python main.py
    volumes:
        - ./vismooc-data-server:/src
    depends_on:
        - db
    networks:
        - back-tier

  redis:
    image: redis
    networks:
      - back-tier

  db:
    image: mongo
    networks:
      - back-tier

networks:
  front-tier:
    driver: bridge
  back-tier:
    driver: bridge
